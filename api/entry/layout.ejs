<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="initial-scale=1, minimum-scale=1,maximum-scale=1, user-scalable=0, width=device-width, height=device-height" />
    <meta name="format-detection" content="telephone=no" />
    <!-- <meta http-equiv="Content-Security-Policy" content="default-src *; img-src *; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'"> -->
    <title>Justos Somos Un Bosque</title>
    <link href="<%= entryFilename %>.css" rel="stylesheet">
</head>
  <body>
    <section id="home">
  		<%- include(sectionHtml) %>
  	</section>
    <script type="text/javascript" src="<%= entryFilename %>.js"></script>

    <% if (typeof userMail !== 'undefined') { %>
      <script type="text/javascript">
        window.$tate('user').value = {mail: '<%= userMail %>'}
      </script>
    <% } %>
    
    <% if (typeof formNotification !== 'undefined') { %>
      <script type="text/javascript">
        window.$tate('<%= formNotification.path %>').value = JSON.parse('<%- formNotification.value %>')
      </script>
    <% } %>

  </body>
</html>